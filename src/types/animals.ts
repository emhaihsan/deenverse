export interface QurbanAnimal {
    id: string;
    name: string;
    price: number;
    maxShares: number;
    minAge: string;
    description: string;
    icon: string;
    color: string;
    benefits: string[];
  }

 